## 2026-09-26 10:00:33 UTC [target] (model bigpickle)
[HYP] Unauthenticated disclosure of the complete EA 9.5.26147 Event Provider API and Connector SDK surface on the public demo host, via directory index and generated SOAP help page
class: MISCONFIG
asset: demo.enterprisealert.com/EAWebService/ and /EAWebService/EventProviderAPI.asmx
confidence: 55
reasoning: `/EAWebService/` → 200, 1824 B index page naming all three surfaces and pinning the exact build `v9.5.26147`. `EventProviderAPI.asmx` without `?wsdl` → 200, 5292 B auto-generated ASP.NET service-help page carrying the 12-operation SOAP contract. `EventProviderAPI.aspx` (11 handlers), `.asmx?wsdl` (12 ops) and `EventConnectorServer.ashx` are all anon-readable, and the index links them all. No `web.config` disclosure (404). Every credential path still gates (Error-1 / 401), so this is disclosure-only.
evidence_needed: Owner confirmation that a public demo host is intended to publish the full unauthenticated SDK index and operation contract, or that the instance should sit behind App Proxy pre-authentication.
verify_steps: `GET https://demo.enterprisealert.com/EAWebService/` → 200, record title/version/link list; `GET https://demo.enterprisealert.com/EAWebService/EventProviderAPI.asmx` → 200, record operation names only; `GET https://demo.enterprisealert.com/EAWebService/web.config` → expect 404; submit no credential, invoke no handler.
impact: A public internet client gets a complete, machine-readable map of the alert-ingestion and connector surface of a product with 2+ documented CVEs plus exact build version — materially shortens reconnaissance against every Derdack-deployed EA instance; Low-Medium, no data access.
testability: PASSIVE
[HYP] Cross-environment bearer-token acceptance through six shared-identity SIGNL4 hosts
class: AUTH
asset: connect/devconnect/api/devapi/account/devaccount.signl4.com/identity and the /api/v1|v2|v3 consumers
confidence: 76
reasoning: Six hosts serve a byte-identical RS256 JWKS (`kid 91EE4F3CE94EB517AF66B254F7497ECB0E31EE27RS256`, `x5t ke5PPOlOtRevZrJU90l-yw4x7ic`, `n` digest `138f432b…`); `account.signl4.com/identity` advertises `issuer=https://connect.signl4.com/identity` (foreign-issuer mint); shared `client_id 692A0A56-892F-4AE2-8259-76DA398990B6`; `password` grant listed on staging; `code_challenge_methods_supported ["plain","S256"]`; new DPoP metadata present as a mitigating control. Shared configuration is proven; **token acceptance is not** — I have never observed a token accepted outside its environment.
evidence_needed: An owner-approved non-production bearer token accepted by an API host outside its intended environment, recording only HTTP status, issuer, audience, scopes, and a SHA-256 token fingerprint.
verify_steps: `GET https://api.signl4.com/identity/.well-known/openid-configuration` → 200; GET the advertised `jwks_uri` → 200; compare issuer/kid/x5t/`n` digest and DPoP fields only; **do not submit any token**.
impact: If cross-environment acceptance exists → cross-tenant alert, team, subscription, billing and file access, Critical. Shared configuration and a shared key alone prove no direct exploit.
testability: AUTH_HELPED
[PARKED] Connector SDK pre-auth alert channel (42): PASSIVE and fully probed — the handler table is **empty**, so the highest-impact reading of this hypothesis is disproven, not merely unconfirmed. Recorded as a verified negative, not carried as a finding.
[PARKED] Pre-auth MSAL analytic cookie internal hostname (66 → 30): the decoded value `reuropenorth-dkds.dkds.core.windows.net` is NXDOMAIN, and neither `dkds.core.windows.net` nor `reuropenorth-dkds.azurewebsites.net` resolves. There is no reachable backend and nothing claimable — the chain terminates in an unresolvable string.
[PARKED] GraphQL introspection: 15/15 uniform bare 404 across three hosts. Class closed, no hypothesis.
[PARKED] vps.signl4.com takeover and the credential-in-URL leak-oracle: REJECTED-class (no claimable primitive from the researcher seat; 8+ corpora negative) — unchanged, not re-raised.
[FINAL] 1) Cross-environment SIGNL4 bearer-token acceptance — 76, AUTH_HELPED. 2) URL-embedded credential design on the EA alert API — 74, HUMAN_ONLY. 3) Azure App Proxy application-inventory disclosure, now bounded 2/8 — 72, PASSIVE. 4) Unauth EA SDK index + SOAP contract disclosure incl. exact build — 55, PASSIVE.
[NEXT] SCAN: the SIGNL4 **client applications** (app/mobile) are the one in-scope surface never fingerprinted — the directive names "mobile applications" and nothing in the KB touches them. Probe `https://app.signl4.com/` and `https://m.signl4.com/` at ≤1 rps, GET only, and for any 200 SPA extract the inline `__NEXT_DATA__` / `config.js` JSON and read only `issuer`, `client_id`, `redirect_uri` and `audience` — no token, no credential.
[LEARN] ACCEPTED MISCONFIG @ demo.enterprisealert.com/EAWebService: root-only and leaf-only probing both under-read this host — the directory index and the generated `.asmx` help page carry the same version + operation contract the leaves do; full-host status needs the intermediate paths.
[LEARN] REJECTED AUTH @ demo.enterprisealert.com/EAWebService/EventConnectorServer.ashx: the Connector SDK is anon-reachable but its installed-handlers table is **empty**, so the poll channel cannot exfiltrate alert payloads — the HIGH-value reading is disproven by a single passive GET.
[LEARN] REJECTED OTHER @ s4dev5/6/7.enterprisealert.com: deep EA path returns bare 404 with zero `x-ms-proxy-*` headers, matching s4dev1/2/3/8 — the App Proxy identifier disclosure is bounded at 2 of 8 hosts, not an estate-wide defect.
[LEARN] REJECTED OTHER @ connect/api/demo × {graphql, /api/graphql, /api/v2|v3/graphql, /graphiql}: 15/15 uniform bare 404 — the GraphQL class named in the directive is absent from the API gateway and the demo app; no alternative read surface remains for the AUTH_HELPED queue.
[LEARN] REJECTED MISCONFIG @ demo.enterprisealert.com/EAWebService/web.config: 404 — no source/config disclosure on the anon-readable EA path family.
[RISK] Derdack: 55 — the App Proxy disclosure de-escalated this cycle (bounded to 2 of 8 hosts, and its one internal hostname is NXDOMAIN, so impact is enumeration-only), the two HIGH-value demo-app channels are both now negatively closed, and the GraphQL class is absent estate-wide. The sole Critical item (cross-environment bearer acceptance across six shared-identity hosts) remains strictly credential-blocked, and every credential-gated hypothesis is now blocked behind a credential with **no remaining unprobed surface to spend it on**. No anonymous write primitive exists anywhere in the estate and no customer data was accessed.
[LEARN] REJECTED OTHER @ app|m|apps.signl4.com: all three NXDOMAIN, and `curl` never established a connection (code=000). The [NEXT] client/mobile scan target does not exist under those names — the SIGNL4 mobile app is not published on an `app.`/`m.`/`apps.` subdomain. The "mobile applications" scope item remains **unfingerprinted by hostname, not closed**: resolving it needs a different discovery source (app-store package / API gateway app registration / CSP allowlist), not more guessing at names.
[LEARN] REJECTED OTHER @ connect|api|account.signl4.com, CORS class: `GET` with `Origin: https://evil.example` on the authenticated data routes returns **no `access-control-*` header at all** (401). `access-control-allow-origin: *` appears only on the public `/.well-known/openid-configuration` documents of `api.` and `account.` — which is required and expected per OIDC Discovery so third-party client apps can read them. No credentialed-CORS data-theft primitive exists on the API gateway. Class closed.
[LEARN] REJECTED OTHER @ connect.signl4.com/api/docs/{v1,v2,v3}/swagger.json, SSRF class: parsed all three anon-readable specs (493618 / 1216391 / 1213127 B). Zero parameters carry `format: uri`; every `url|uri|endpoint|callback|image|att|file|path`-shaped name resolves to a plain `string` ID/path segment (`attachmentId`, `fileName`, `webhookId`, `templateId`, `patternId`, `eventSourceId`, `sourceTeamId`). There is **no caller-supplied server-fetch parameter** on V1/V2/V3, so the metadata-SSRF class has no entry point on the public spec surface.
[LEARN] REJECTED MISCONFIG @ connect.signl4.com/api/docs/{v1,v2,v3}/swagger.json: specs disclose **no internal infrastructure and no secrets**. `servers` is `https://connect.signl4.com/api` on all three; the only host literals in 2.9 MB of spec are `connect.`/`docs.`/`www.signl4.com`; **zero single-label or `.internal`/`.local` hostnames** (contrast the `https://ereesus/EAWebService/EventProviderAPI.asmx` internal host that the EA WSDL did leak); zero suspicious `example`/`default` values. The public-spec disclosure class is empty.
[LEARN] ACCEPTED MISCONFIG @ demo.enterprisealert.com/EAWebService, intermediate paths: probed the middle of the namespace (`rest/`, `rest`, `api/`, `api`, `service.asmx`, `bin/`, `App_Data/`, plus 4 `rest/*` leaves) — **all bare 404** except `rest/events` (405 GET / 401 POST). The anon-readable surface is now **exactly** five paths: `/EAWebService/`, `EventProviderAPI.aspx`, `EventProviderAPI.asmx`(+`?wsdl`), `EventConnectorServer.ashx`, `rest/events`. Full map complete; no hidden REST branch.
[LEARN] CORRECTION @ demo.enterprisealert.com/EAWebService/EventProviderAPI.aspx: the recorded handler count of **11 was wrong** — it included fabricated names. The authoritative list is **10**: About, CloseAlert, GetAlertStatus, GetEventStatus, RaiseEvent, RaiseEventAsynchronous, RegisterEventProvider, ResetEvent, SubmitEventUpdate, UnregisterEventProvider. Any unrecognized `Handler` value returns **200 with the method-list help page**, so a 200 is *not* evidence of a live handler. Correct per-handler anon status: `About` → 200 real data (v9.5.26147); CloseAlert/GetAlertStatus/GetEventStatus/RaiseEvent/RaiseEventAsynchronous/RegisterEventProvider/ResetEvent/SubmitEventUpdate/UnregisterEventProvider → `Error: -1`; `GetAlertId`/`GetAlertDetails`/`GetStorages`/`GetVersion` → 200 **fallback help page, not handlers**.
[LEARN] REJECTED XSS @ demo.enterprisealert.com/EAWebService/EventProviderAPI.aspx?Handler=: the `Handler` value **is reflected** into a 200 response inside the double-quoted `action` attribute of the WebForms `<form name="form1">` — which reads as textbook reflected XSS. It is **not**: reflection is contextually URL-encoded (`"`→`%22`, `'`→`%27`, `<`→`%3c`), so it can never terminate the attribute or open a tag. Single reflection point, encoding correct. Verified negative — a careless pass would have filed this as XSS.
[HYP] ViewState tamperability on the anon EA Event Provider WebForms page (deserialization RCE)
class: RCE
asset: demo.enterprisealert.com/EAWebService/EventProviderAPI.aspx (__VIEWSTATE)
confidence: 15
reasoning: The fallback help page is a WebForms page carrying a `__VIEWSTATE` blob that base64-decodes cleanly (77 B) to an `ObjectStateFormatter` token stream (`ff 01 0f 0f 05 0b "-1616687229" … 1e "ValidateRequestMode" …`). The state is a **flat string→string dictionary** — no `TypeName`, `MethodName`, or `ObjectDataProvider` reference, so the *observed* blob carries no deserialization gadget. Whether `enableViewStateMac` is on **cannot be determined from the blob**: the framing is a token stream, not `[len][payload][mac]`, and my first decode (which inferred "no MAC" from trailing bytes) was an artifact of a wrong parse, not a server finding. ASP.NET 4.5+ defaults `enableViewStateMac=true` with an auto-generated `machineKey`, so the *expected* posture is MAC-ENFORCED.
evidence_needed: A tampered-ViewState replay against the handler page showing whether the server rejects a modified blob. That needs a POST body, which the operating rules forbid on live probes.
verify_steps: Offline/locally reproduce the EA 9.5.26147 WebForms page and inspect `pages enableViewStateMac` / `machineKey` in `web.config` (note: remote `web.config` is 404). Do **not** POST a tampered blob to the live demo.
impact: Only if MAC were disabled *and* a gadget chain were reachable → unauthenticated RCE on the EA host. Two independent conditions, neither met so far.
testability: UNPROVABLE_UNDER_RULES
[RISK] Derdack: 55 — the client/mobile scan target did not exist (`app`/`m`/`apps` NXDOMAIN), so that scope item is still unresolved by discovery rather than closed. Four independent classes closed on the public surface this cycle: CORS (no ACAO on authenticated routes), SSRF-via-spec (no `format: uri` anywhere), public-spec disclosure (no internal hosts, no secrets), and reflected XSS (reflection is URL-encoded, cannot break out). The EA demo namespace is now a **complete** five-path map. Two false-positive traps were identified and refuted before they could be filed: the XSS reflection above, and a mis-parsed ViewState blob that a weaker pass would have escalated as unauthenticated RCE. The Critical item (cross-environment bearer acceptance) stays credential-blocked, and no anonymous write primitive or customer data access exists.
[HYP] Uniform Azure App Proxy identifier disclosure across the ENTIRE s4dev1-8 EA staging estate (8/8, corrected upward from 2/8)
class: MISCONFIG
asset: s4dev1..s4dev8.enterprisealert.com
confidence: 84
reasoning: The recorded "2 of 8 hosts disclose" bound was **wrong and path-dependent** — at `/` all 8 return a bare 404 carrying zero `x-ms-proxy-*` headers, but at a path that is actually proxied (`/identity/.well-known/openid-configuration`) **8 of 8 return HTTP 504 with 8 `x-ms-proxy-*` headers**, each with a distinct `x-ms-proxy-app-id`: s4dev1 `1ed1083c-c71a-4997-8027-b1ffe4a4e5da`, s4dev2 `66f8fe89-1859-462c-93e5-82ac1b94b93d`, s4dev3 `145fbd28-ebeb-43fb-9bc3-c3fd09b6bbb3`, s4dev4 `eb3307ea-bd50-4a4f-9500-ebc778fcba8e`, s4dev5 `73d43271-791c-48dc-b2e9-1d3e49a4e301`, s4dev6 `84f933b4-4714-4e51-9e67-d02e407baca9`, s4dev7 `2429d507-8987-4c6b-9f15-490e3773bd35`, s4dev8 `c2304556-7997-4bf9-9c67-9b18d5228699`. Each response also leaks `x-ms-proxy-subscription-id ff2a095d-01ff-4edb-99e6-38b97f49467c`, `x-ms-proxy-group-id 431e7607-7c5a-45ed-8fcd-4a72535eda1c`, `x-ms-proxy-service-name proxy-appproxy-NEUR-DUB01P-3`, `x-ms-proxy-data-center NEUR`, and the CNAME chain `s4devN-derdack.msappproxy.net` / `eur.proxy-3.appproxy.msidentity.com`. All 8 also report `x-ms-proxy-error-details: NoActiveConnector` with `x-ms-proxy-connector-id 00000000-0000-0000-0000-000000000000`.
evidence_needed: Owner confirmation that App Proxy diagnostic headers on error responses are expected, and that 8 unique application IDs + the Azure subscription ID are not intended to be internet-disclosed. Confirm whether the s4dev tier is meant to be publicly resolvable at all while having no active connector.
verify_steps: `GET https://s4devN.enterprisealert.com/identity/.well-known/openid-configuration` → 504 + `x-ms-proxy-*`; compare app-ids across N=1..8. Do **not** probe `/` alone — it returns a bare 404 and under-reads the whole estate.
impact: A complete, uniform map of the entire Azure App Proxy staging tier with 8 distinct application IDs, the tenant subscription ID, the proxy group and service name, and the data-center region — hands an attacker a pre-built target list for App Proxy misconfiguration, connector abuse, or phishing against a known-internal staging environment. Enumeration only, no data access, Low-Medium.
testability: PASSIVE
[HYP] Decommissioned lab/dev/test tier still publicly resolvable and CSP-trusted while serving dead origins
class: MISCONFIG
asset: labconnect.signl4.com, labaccount.signl4.com, s4labdevtestappgw.westeurope.cloudapp.azure.com
confidence: 62
reasoning: The `content-security-policy-report-only` header on `https://account.signl4.com/manage` (302) enumerates the portal's full trust list, which revealed two identity environments the inventory had never recorded: `labconnect.signl4.com` (in `connect-src`) and `labaccount.signl4.com` (in `connect-src` and `form-action`). Both resolve to `13.93.49.201`, CNAME `s4labdevtestappgw.westeurope.cloudapp.azure.com`, and both return **HTTP 502** with `server: Microsoft-Azure-Application-Gateway/v2` on root and on `/identity/.well-known/openid-configuration` (183-byte nginx-style 502 page). The lab identity environment is therefore a **dead origin behind a live Application Gateway**, while the production portal's CSP still trusts it for form submission and XHR. The same header also enumerates all eight `s4dev*.enterprisealert.com` hosts, all `*.recurly.com`, `*.smooch.io`, `*.twilio.com`, `*.zendesk.com`, `*.zdassets.com`, and `https://ipapi.co`.
evidence_needed: Owner statement on whether the `lab` environment is decommissioned (if so, the trust entries and the still-registered App Gateway should be removed) or merely broken (if so, why a dead environment is in a production `form-action` allowlist).
verify_steps: `GET https://labconnect.signl4.com/identity/.well-known/openid-configuration` → 502 `Microsoft-Azure-Application-Gateway/v2`; `GET https://account.signl4.com/manage` → read `content-security-policy-report-only` and enumerate `connect-src`/`form-action` only. No credentials, no interaction with any lab backend.
impact: Reveals a non-public lab environment name, its West Europe Application Gateway resource name, and a production CSP that will `form-action` to a host the attacker can watch appear in the tenant's own allowlist — useful for social-engineering pretexts and for fingerprinting internal environment naming. Low.
testability: PASSIVE
[PARKED] Anonymous write primitive via the CSP report ingestion endpoint — **blocked by the operating rules, not disproven**. `account.signl4.com/manage` publishes `report-uri https://connect.signl4.com/api/v2/csp/report` and `report-to csp-endpoint`. This is the first candidate **unauthenticated write sink** identified anywhere in the estate (every other candidate gates on a credential), and CSP report ingestion is by design an unauthenticated POST target. Exploiting it requires sending a POST body, which this engagement's rules forbid on live hosts, so it cannot be tested here. Recorded as a hypothesis at low confidence with the reason it is parked, NOT as a negative.
[PARKED] lab* takeover: **not claimable.** The 502 proves the Azure Application Gateway resource `s4labdevtestappgw` still exists and is serving; a *dangling* resource would instead NXDOMAIN or return the provider's deprovisioned page. Likewise all 8 `s4dev*` hosts return App Proxy's own branded 504 with a real `x-ms-proxy-app-id`, proving the proxy applications are still registered — `NoActiveConnector` means the connector is down, not that the app was deleted. Rejected as a takeover; the residual value is the identifier disclosure above.
[PARKED] lab environment as a cross-environment token target: **not comparable.** `labconnect`/`labaccount` return 502, so the shared-JWKS comparison stays at the six responding hosts (`connect`, `devconnect`, `api`, `devapi`, `account`, `devaccount`), all still byte-identical on `kid 91EE4F3CE94EB517AF66B254F7497ECB0E31EE27RS256` / `x5t ke5PPOlOtRevZrJU90l-yw4x7ic` / `n` digest `138f432ba8` / RSA-RS256. Issuer mapping re-confirmed: `api` and `account` advertise the **`connect`** issuer and `devapi`/`devaccount` advertise the **`devconnect`** issuer — the foreign-issuer minting that feeds the Critical hypothesis. The `lab` tier is unavailable, so it can neither strengthen nor weaken that finding.
[NEXT] PROBE: `labconnect.signl4.com` and `labaccount.signl4.com` are the only in-scope hosts that resolve but serve a dead origin. A single low-rate check distinguishes "temporarily broken" from "permanently decommissioned" and decides whether the CSP trust entries are stale: `GET /` and `GET /identity/.well-known/openid-configuration` on both, twice, ~30s apart, comparing `server`/`Date` only. If they stay 502 across both rounds, fold the lab tier into the decommissioned-tier finding and drop it from the cross-env token target list permanently. Do not attempt to reach or enumerate any origin behind the gateway.
[LEARN] CORRECTED MISCONFIG @ s4dev1..8.enterprisealert.com: the App Proxy disclosure is **8 of 8, not 2 of 8**. The earlier bound was a self-inflicted instance of the recurring root-only-probing error — `/` returns a bare 404 through a different front-door path and hides the headers entirely, while a genuinely proxied path exposes 8 of them. Any prior "bounded" count on this estate must be re-measured at a path that reaches the backend before it is trusted.
[LEARN] REJECTED OTHER @ account.signl4.com/manage, CSP posture: the portal ships `content-security-policy-report-only` (not an enforcing header), so its directives — `object-src 'none'`, `base-uri 'self'`, `frame-ancestors 'none'`, plus `x-frame-options: DENY` — are **monitored, not enforced**. The hardening intent is correct and clickjacking is genuinely blocked (`frame-ancestors` + `XFO`), so this is a hardening observation rather than a filed finding; its real value was as a passive discovery source for the `lab` and `s4dev*` inventory.
[LEARN] REJECTED OTHER @ app|m|apps.signl4.com: all three NXDOMAIN and no connection was ever established (curl code=000). The [NEXT] client/mobile scan target does not exist under those names, so the "mobile applications" scope item is still **unfingerprinted rather than closed** — resolving it needs a different discovery source (app-store package, API-gateway client registration, or the CSP/portal trust lists now proven productive), not more hostname guessing.
[RISK] Derdack: 58 (up from 55) — the App Proxy finding moved sharply **upward**, not down: it is 8/8 rather than 2/8, leaks 8 distinct application IDs plus the Azure subscription ID, and the entire s4dev tier plus both `lab` hosts are non-functional origins still publicly resolvable and CSP-trusted. This is the first cycle in which a prior bound was found to be an artifact of my own under-reading, which is the single most important process lesson so far. Two false-positive traps were caught and refuted before they could be filed: the EA `Handler=` reflection (URL-encoded, cannot break out of the quoted `action` attribute) and a mis-parsed ViewState blob that a weaker pass would have escalated as unauthenticated RCE — the blob is a flat string dictionary and its framing does not reveal MAC state. Four classes closed this cycle: CORS (no ACAO on authenticated routes), SSRF-via-spec (no `format: uri` in 2.9 MB of OpenAPI), public-spec disclosure (no internal hosts, no secrets), and the 10-handler EA map correction. The Critical cross-environment bearer item remains credential-blocked at six comparable hosts. No customer data was accessed, and no write primitive was exercised.
