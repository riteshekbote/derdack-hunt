## 2026-09-22 00:09:56 UTC [target] (model bigpickle)
[HYP] EA REST apiKey-in-URL is vendor-documented primary auth — org template family fully corroborated by product docs (CONSOLIDATED finding)
class: MISCONFIG
asset: github.com/Derdack/{User-Monitoring, nagios, 2wayREST×3, SIGNL4} + www.derdack.com Webhook/REST-API page + enterprisealert.zendesk.com CURL article
confidence: 78
reasoning: Vendor portfolio page literally instructs replacing `key` in `http://server-name/EAWebService/rest/events?apiKey={API-Key}`; Zendesk article (2024-04-15) documents raise/update/reset on same path family with apiKey in query; 6/12 org repos render the identical pattern (`User Monitoring.ps1` L42 `<REST_Endpoint_Key>`); key created once, copy-pasted into cleartext URL, persists in scripts/process args long-term. Key format `l1slfpjwirbm6twzs30bhtcl3z34m8v9` = lowercase-alnum, operator-genera ted (not high-entropy prefix-gated).
evidence_needed: deployed-instance evidence of live customer key reaching EA access-log/process-list surface (HUMAN, on-prem).
verify_steps: DONE (raw GET of 2 vendor pages + 6 repo entry points + search index digest). Residual: deployed-instance only.
impact: EA log/access-log/process reader recovers a live REST Endpoint Key → unauth alert read/injection/close/reset on customer EA — Medium, deployment-side.
testability: PASSIVE (verified) with HUMAN residual
[HYP] Vendor REST integration template disables TLS by default (`-L -k`, `http://` URLs) — key-in-query-string travels cleartext on-wire and into proxy/access logs
class: MISCONFIG
asset: enterprisealert.zendesk.com CURL article + derdack.com Webhook/REST-API page (on-prem EA REST source provisioning)
confidence: 55
reasoning: Every vendor curl example uses `-k` (verify skipped) + `http://localhost` scheme; state-mutating paths `/rest/events/{id}/update|reset` are GET/`--data` with the apiKey in the URL query, so the secret lands in web/proxy/access-log URL lines regardless of transport; portfolio page shows same `http://` example. No TLS-enforcement or `https only` note in either source.
evidence_needed: operator following template on HTTP or with `-k` where an intermediate proxy logs URLs (HUMAN, deployment-side).
verify_steps: DONE (raw GET of article digest + portfolio page; article body 403 on direct fetch, digest suffices).
impact: On-wire/at-rest secret recoverability for EA REST keys in customer deployments — Low/Medium, design-side corroborator for family finding.
testability: PASSIVE (verified) with HUMAN residual
[HYP] checkmk 2-way plugin hardcoded credential reuse (cmkadmin/CNlydVqZ)
class: MISCONFIG
asset: github.com/Derdack/derdack-plugin-checkmk/2-way/Main.js
confidence: 55
reasoning: L90-91 hardcoded username/password (sha256 beb9fa72…); L330/373 log full "Checkmk response: " bodies incl auth results; RFC1918 target not remotely reachable — leak-only, same copy-paste operator family as apiKey templates.
evidence_needed: operator reuse of template cred (HUMAN, historical/leak-only).
verify_steps: DONE (raw GET). No live probe possible.
impact: Checkmk API account compromise on reusing deployments — Low/Medium, disclosure-side.
testability: PASSIVE (verified) with HUMAN residual
[NEXT] RAG: sweep docs.enterprisealert.com + enterprisealert.zendesk.com for a real customer-deployed EA REST key or the exact article example `l1slfpjwirbm6twzs30bhtcl3z34m8v9` to test leak-oracle (one query: `"EAWebService/rest/events" apiKey` on Sourcegraph/grep.app); expect negative — then finalize family report as PASSIVE-verified with HUMAN residual.
[RISK] Derdack program: 55 — live anonymous surface exhausted (all 401/404/405/411, zero drift); strongest finding = vps.signl4.com takeover [95] (nemotron3) + deployment-side org credential-template family [78] now vendor-doc-backed across 3 sources and 6/12 repos; AUTH_HELPED queue (cross-env forgery, V1 userId IDOR, V3 BOLA) remains blocked on legitimate credential; RAG oracle negative again — no live-key leak recovered, family impact capped at deployment-side Medium.
