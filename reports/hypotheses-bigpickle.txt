## 2026-09-26 00:09:04 UTC [target] (model bigpickle)
[HYP] Azure AD App Proxy leaks a multi-application inventory, connector state and region topology to unauthenticated clients
class: MISCONFIG
asset: demo.enterprisealert.com/EAWebService/ + s4dev4.enterprisealert.com/EAWebService/
confidence: 80
reasoning: demo `Handler=About` → 200 unauth with `subscription-id ff2a095d-01ff-4edb-99e6-38b97f49467c`, `app-id f18ba879-3dea-4018-b9d1-e6dbdc9ce151`, `group-id 6b26e6a4-c5ad-4b27-b31c-e1777730c354`, `connector-id da867a92-3ba5-4e78-95f6-c97bac3c791f`, `service-name proxy-appproxy-NEUR-DUB01P-3`, `data-center NEUR`. s4dev4 same path → 504 with the **same subscription GUID and region** but `app-id 1f22d4e0-b2e2-4618-9dd7-f0214bbd6ed5`, `group-id 431e7607-7c5a-45ed-8fcd-4a72535eda1c`, `connector-id 00000000-0000-0000-0000-000000000000`, plus `x-ms-proxy-error-details: NoActiveConnector`. s4dev1/2/8 → 404 with zero proxy headers. Only `transaction-id` rotates.
evidence_needed: Owner confirmation that App Proxy emits these to unauthenticated clients by default (no per-app override) and the operator's GUID→App Service/connector mapping; report GUIDs only.
verify_steps: GET `https://demo.enterprisealert.com/EAWebService/EventProviderAPI.aspx?Handler=About` → 200, capture only `x-ms-proxy-*` + `NEL`; GET `https://s4dev4.enterprisealert.com/EAWebService/EventProviderAPI.aspx?Handler=About` → 504 with `x-ms-proxy-error-details`; submit no credential, invoke no handler.
impact: Attacker enumerates Derdack's Azure subscription GUID, ≥2 distinct App Proxy applications, their group/connector IDs, which connector pools are live vs `NoActiveConnector`, and the NEUR-DUB01P-3 hosting region — enables targeted Azure resource enumeration and subscription-scoped phishing against Derdack cloud admins; Low-Medium, no data access.
testability: PASSIVE
[HYP] Pre-auth MSAL analytic cookie discloses the internal Azure App Service default hostname
class: MISCONFIG
asset: demo.enterprisealert.com (any 200 path under /EAWebService/)
confidence: 66
reasoning: Unauthenticated `GET .../EventProviderAPI.aspx?Handler=About` returns `Set-Cookie: AzureAppProxyAnalyticCookie_f18ba879-..._https_1.3=MGD:<base64>` with `path=/; Secure; SameSite=None`. Base64-decoding the `MGD:` payload yields the printable string `reuropenorth-dkds.dkds.core.windows.net` alongside the claim URI `http://schemas.xmlsoap.org/ws/2005/05/identity/claims/name` and value `cwapproxydkdseur0`. The `f18ba879-…` app-id in the cookie name matches the leaked `x-ms-proxy-app-id`, confirming the two disclosures are the same App Proxy app.
evidence_needed: Owner confirmation that the App Service default hostname is not intended for external disclosure and is not in scope for a self-service claim path (`*.dkds.core.windows.net` is tenant-derived, not a dangling resource).
verify_steps: GET `https://demo.enterprisealert.com/EAWebService/EventProviderAPI.aspx?Handler=About` → 200; base64-decode only the `MGD:` cookie value; read printed strings; submit no credential, invoke no handler.
impact: Attacker learns the internal App Service name (`reuropenorth-dkds`) and that the app front is App Service-backed, correlating with the leaked app-id to name the backend App Service; Low, topology aid only, resource is not claimable from the researcher seat.
testability: PASSIVE
[HYP] Cross-environment bearer-token acceptance through six shared-identity SIGNL4 hosts
class: AUTH
asset: connect/devconnect/api/devapi/account/devaccount.signl4.com/identity and /api/v1|v2|v3 consumers
confidence: 72
reasoning: All six hosts serve byte-identical RS256 JWKS (`kid 91EE4F3CE94EB517AF66B254F7497ECB0E31EE27RS256`, `x5t ke5PPOlOtRevZrJU90l-yw4x7ic`, CN=`*.signl4.com`); `account.signl4.com/identity` advertises `issuer=https://connect.signl4.com/identity` (foreign-issuer mint); both discover `code_challenge_methods_supported ["plain","S256"]`; shared `client_id 692A0A56-892F-4AE2-8259-76DA398990B6`; `password` grant listed on staging. Fresh discovery now also advertises DPoP signing-algorithm metadata — a mitigating control, and a partial bar to naive bearer replay. No cross-env acceptance has been observed.
evidence_needed: An owner-approved non-production bearer token accepted by an API host outside its intended environment, recording only HTTP status, issuer, audience, scopes and a SHA-256 token fingerprint.
verify_steps: GET `https://api.signl4.com/identity/.well-known/openid-configuration` → 200; GET the advertised `jwks_uri` → 200; compare issuer/kid/x5t/`n` digest and DPoP fields only; do not submit any token.
impact: If cross-env acceptance exists → cross-tenant alert/team/subscription/billing/file access, Critical; shared configuration plus a shared key alone proves no direct exploit.
testability: AUTH_HELPED
[NEXT] PROBE: GET `https://s4dev5.enterprisealert.com/EAWebService/EventProviderAPI.aspx?Handler=About` and capture only `x-ms-proxy-*`; then `s4dev6` and `s4dev7` at ≤1 rps. Expected 504 with a third distinct `app-id`/`group-id` under subscription `ff2a095d-…` (a 3rd mapped app, widening the inventory) or bare 404 (bounds the disclosed app count at 2/8). This bounds the App Proxy app inventory claimed in finding 1 instead of leaving it at 2 sampled hosts.
[RISK] Derdack: 60 — the App Proxy disclosure widened from one demo-only app to two distinct applications plus a connector-liveness fault channel, and now also leaks the internal App Service hostname pre-auth, making it a defensible Low-Medium submission; however it grants no data access and no claimable resource. The single potentially Critical item (cross-environment bearer-token acceptance across six shared-identity hosts) remains strictly credential-blocked and is now further gated by DPoP metadata, and the EA credential-in-URL family still has no proven live leak across 8+ corpora. No anonymous write primitive was exercised and no customer data was accessed.
