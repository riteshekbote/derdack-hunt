## 2026-09-26 14:00:40 UTC [target] (model bigpickle)
class: AUTH
asset: connect|api|devconnect.signl4.com/mobileapi + /identity/connect/token
confidence: 45
reasoning: The mobile API is a **separate Azure App Gateway v2 ingress** on three hosts while every documented API route is Kestrel `Microsoft-HTTPAPI/2.0` — two front doors on one hostname. `mobile_api` is a first-class scope in the 12-scope set on **both** `connect` and `account` (issuer `https://connect.signl4.com/identity`), which is shared byte-for-byte by six identity hosts on one RS256 key. The gateway deny is uniform and byte-identical for real, fabricated, swagger and Bearer-bearing requests, so it is a **network-position gate, not an authorization gate** — it constrains where the mobile API is reachable from, not who may use it. DPoP is advertised but **not required** (`dpop_bound_access_tokens_required` absent), and PAR is available but not mandatory. The browser/portal path can therefore mint a `mobile_api` token with no sender-constraint to tie it to the mobile ingress.
evidence_needed: One owner-approved non-production token with the `mobile_api` scope presented to (a) the `/mobileapi` ingress from an allowed position and (b) the Kestrel ingress at `/api/v2/*`, recording only HTTP status, `iss`, `aud`, `scp` and a SHA-256 token fingerprint. Acceptance on the Kestrel ingress with a mobile-only scope is the proof; shared-configuration facts alone are not.
verify_steps: `GET https://connect.signl4.com/identity/.well-known/openid-configuration` → confirm `mobile_api` in `scopes_supported` and absence of `dpop_bound_access_tokens_required`; `GET https://connect.signl4.com/mobileapi` → 403 `Microsoft-Azure-Application-Gateway/v2`, 79088 B, sha256 `12405c95d3c31de4`; compare against `GET https://connect.signl4.com/api/v2/teams` → 401 `Microsoft-HTTPAPI/2.0`, 0 B. **Do not submit any token.**
impact: Mobile scope = responder on-call access to live alerts, acknowledgements and push dispatch for every team on the subscription. If the scope is accepted off its intended ingress, a browser-issued token crosses the network boundary the App Gateway is there to enforce → account/alert takeover across a mobile-only trust zone. Critical **if** confirmed; unproven today.
testability: AUTH_HELPED
class: AUTH
asset: connect|devconnect/api|devapi/account/devaccount .signl4.com/identity
confidence: 42
reasoning: `backchannel_logout_supported: true` is now recorded on the discovery documents of both `connect` and `account`, and the account-hosted IdP advertises the **connect** issuer. All six hosts publish byte-identical JWKS (`kid 91EE4F3CE94EB517AF66B254F7497ECB0E31EE27RS256`), so a logout_token minted by any one of them verifies against all six. The environment-scoped `sid`/`iss` split that would contain a logout to its own environment is exactly the control that has already been shown absent in the mint path (account claims the connect issuer). `offline_access` is granted, so refresh tokens exist to outlive a logout that does not propagate.
evidence_needed: An owner-approved client with a registered `backchannel_logout_uri`, plus a session in one environment, demonstrating whether a logout_token from host A is honoured by host B. Requires a credential and an interactive login; not reachable by passive probe.
verify_steps: `GET https://connect.signl4.com/identity/.well-known/openid-configuration` → read `backchannel_logout_supported`, `issuer`; `GET https://account.signl4.com/identity/.well-known/openid-configuration` → same field, compare `issuer`. Read only; no token, no login.
impact: Non-propagating logout leaves a refresh-token-bearing session usable in a second environment after the user signed out — session persistence across a trust boundary. High if confirmed, Medium residual. Currently unproven.
testability: AUTH_HELPED
class: MISCONFIG
asset: s4dev1..s4dev8.enterprisealert.com
confidence: 84
reasoning: At `/` all 8 return a bare 404 with zero `x-ms-proxy-*` headers; at a path that reaches the backend (`/identity/.well-known/openid-configuration`) **8 of 8 return 504 with 8 `x-ms-proxy-*` headers**, each with a distinct `x-ms-proxy-app-id` (s4dev1 `1ed1083c-…`, s4dev2 `66f8fe89-…`, s4dev3 `145fbd28-…`, s4dev4 `eb3307ea-…`, s4dev5 `73d43271-…`, s4dev6 `84f933b4-…`, s4dev7 `2429d507-…`, s4dev8 `c2304556-…`). All also leak `x-ms-proxy-subscription-id ff2a095d-01ff-4edb-99e6-38b97f49467c`, `x-ms-proxy-group-id 431e7607-…`, `x-ms-proxy-service-name proxy-appproxy-NEUR-DUB01P-3`, `x-ms-proxy-data-center NEUR`, and `x-ms-proxy-error-details: NoActiveConnector`. The earlier "2 of 8" bound was a root-only-probing artifact of my own making.
evidence_needed: Owner confirmation that App Proxy diagnostic headers on error responses are intended, and that 8 application IDs plus the tenant subscription ID may be internet-disclosed.
verify_steps: `GET https://s4devN.enterprisealert.com/identity/.well-known/openid-configuration` for N=1..8 → 504 + `x-ms-proxy-*`; diff the app-ids. **Never probe `/` alone** — it returns a bare 404 and under-reads the whole estate.
impact: Complete uniform map of the App Proxy staging tier with 8 app IDs, the subscription ID, proxy group, service name and region — a pre-built target list for App Proxy misconfiguration, connector abuse, or phishing against a known-internal staging estate. Enumeration only, no data access. Low-Medium.
testability: PASSIVE
