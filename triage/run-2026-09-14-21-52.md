
===== TRIAGE 2026-09-14 21:52:08 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/{scope.yml,derdack.md}"[90m 2 matches[0m
[0m✱ [0mGlob "**/inventory/*"[90m 1 match[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/derdack.md
The `LEADS:` section is empty in your prompt. I've extracted the substantive findings from the inventory and validated each against the 7-Question Gate below:

---

## Lead 1: Cross-Env JWKS Key Reuse (Staging ↔ Prod)

**Asset:** `devconnect.signl4.com` ↔ `connect.signl4.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** — signl4.com product estate, "All company-owned infrastructure…including SIGNL4" in scope |
| Q2 | **Yes** — OIDC discovery (`/.well-known/openid-configuration`) returns 200 unauthenticated |
| Q3 | **Yes** — shared RS256 signing key + identical `client_id`/scopes between staging & prod means staging compromise → prod token forgery |
| Q4 | **Yes** — GET `/.well-known/openid-configuration` + GET JWKS endpoint; passive only |
| Q5 | **Uncertain** — cross-env key reuse is a common misconfiguration but program-specific; no evidence of prior report |
| Q6 | **No** — not on always-rejected list |
| Q7 | **Yes** — realistic; shared signing key across environments is a meaningful auth bypass vector |

**Verdict: VALID**
- **Proof steps:** `curl -s https://devconnect.signl4.com/.well-known/openid-configuration | jq '.jwks_uri'` → fetch JWKS → `deep_equal` vs prod `connect.signl4.com` JWKS
- **Impact:** Compromise of staging credentials/secrets allows forging valid JWTs for production tenants
- **CVSS 3.1:** AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N → **8.1 (High)**
- **Channel:** bugs.olivermaicher.eu

---

## Lead 2: Password Grant Enabled on Staging IdP

**Asset:** `devconnect.signl4.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** — SIGNL4 staging infrastructure |
| Q2 | **Yes** — token endpoint reachable, OIDC discovery unauthenticated |
| Q3 | **Yes** — ROPC grant enables username/password → token without browser redirect; brute-force/forgery risk if combined with shared key |
| Q4 | **Yes** — GET `/.well-known/openid-configuration` reveals `grant_types_supported` includes `password`; passive |
| Q5 | **Yes** — password grant is deprecated and a specific misconfiguration; unreported |
| Q6 | **No** |
| Q7 | **Yes** — OWASP & OAuth best-practice guides flag ROPC as dangerous |

**Verdict: VALID**
- **Proof steps:** `curl -s https://devconnect.signl4.com/.well-known/openid-configuration | jq '.grant_types_supported'` → confirm `password` present
- **Impact:** Credential-stuffing/brute-force via token endpoint; combined with Lead 1 key reuse → full account takeover chain
- **CVSS 3.1:** AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N → **7.3 (High)**
- **Channel:** bugs.olivermaicher.eu

---

## Lead 3: dev.derdack.com Apache MultiViews Path Disclosure

**Asset:** `dev.derdack.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** — derdack.com subdomain |
| Q2 | **Yes** — HTTP 300 returned unauthenticated |
| Q3 | **Weak** — lists `.ssh/`, `.bash_history/`, `.viminfo/` as "similar documents" but all return 403/404; no file content disclosed; proof the directory exists but files blocked |
| Q4 | **Yes** — GET `/.well-known/` returns 300 with directory listing |
| Q5 | **Yes** — but trivial; Apache MultiViews is a well-known default |
| Q6 | **No** — but close to "Descriptive error messages" exclusion |
| Q7 | **Borderline** — bigpickle already rejected this as "minimal impact, static basename echo" |

**Verdict: INVALID**
- **Reason:** Directory names shown are fabricated basename echoes, not real paths; files are 403/404; no sensitive data disclosed; falls under "Descriptive error messages or headers" out-of-scope

---

## Lead 4: www.derdack.com WP REST API User Enumeration

**Asset:** `www.derdack.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** — derdack.com corporate site |
| Q2 | **Yes** — `/wp-json/wp/v2/users` returns 200 unauthenticated |
| Q3 | **Low** — exposes user display names, slugs, avatar URLs; no email/PII gated |
| Q4 | **Yes** — passive GET |
| Q5 | **No** — WordPress user enumeration via REST API is a standard finding; well-documented |
| Q6 | **No** — but close to "Username enumeration based on login or forgot password pages" exclusion (WP REST API variant) |
| Q7 | **No** — most triagers reject WP user enumeration without additional impact |

**Verdict: INVALID**
- **Reason:** Standard WordPress behavior; out-of-scope "Username enumeration" pattern; no email/PII disclosed; no chaining to higher impact demonstrated

---

## Lead 5: www.derdack.com/de/ XML-RPC Fully Exposed

**Asset:** `www.derdack.com/de/`

| # | Answer |
|---|--------|
| Q1 | **Yes** — WordPress multisite on derdack.com |
| Q2 | **Yes** — POST to `/de/xmlrpc.php` returns 200 with full method list unauthenticated |
| Q3 | **Yes** — pingback.ping (SSRF/DDoS amplifier), system.multicall (brute-force amplifier), wp.uploadFile, wp.getUsers, wp.getMediaLibrary all exposed |
| Q4 | **Yes** — POST with XML body, passive method enumeration |
| Q5 | **Yes** — root xmlrpc.php is 503-blocked but /de/ bypass exists; specific to multisite path |
| Q6 | **No** — "Network level DoS" excluded but XML-RPC amplification/DDoS is distinct; pingback SSRF is distinct |
| Q7 | **Yes** — XML-RPC exposure with pingback + multicall is a accepted finding |

**Verdict: VALID**
- **Proof steps:** `curl -X POST -d '<?xml version="1.0"?><methodCall><methodName>system.listMethods</methodName></methodCall>' https://www.derdack.com/de/xmlrpc.php` → 200 with full method list
- **Impact:** DDoS amplification via pingback.ping; brute-force via system.multicall; SSRF via pingback to internal hosts
- **CVSS 3.1:** AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:H → **7.5 (High)**
- **Channel:** bugs.olivermaicher.eu

---

## Lead 6: blog.derdack.com / techblog.derdack.com HTTPS→HTTP Downgrade

**Asset:** `blog.derdack.com`, `techblog.derdack.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** — derdack.com subdomains |
| Q2 | **Yes** — redirects unauthenticated |
| Q3 | **Weak** — downgrade to HTTP allows MITM but cookies are secure+host-only (verified); no auth cookie theft path |
| Q4 | **Yes** — `curl -I https://blog.derdack.com` → 302 to `http://www.derdack.com` |
| Q5 | **Yes** — but common misconfiguration |
| Q6 | **No** |
| Q7 | **No** — without a demonstrated auth cookie theft chain, this is a best-practice issue |

**Verdict: INVALID**
- **Reason:** No exploitable impact path; cookies verified secure+host-only; falls under "SSL/TLS best practices" out-of-scope

---

## Lead 7: Missing HSTS/CSP/X-Frame-Options on www.derdack.com

**Asset:** `www.derdack.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** |
| Q2 | **Yes** — HTTP response headers readable |
| Q3 | **Weak** — HSTS missing enables SSL stripping; CSP missing enables XSS; X-Frame-Options missing enables clickjacking |
| Q4 | **Yes** — `curl -I` passive |
| Q5 | **No** — very common; most triagers reject missing security headers without exploit chain |
| Q6 | **No** |
| Q7 | **No** — explicitly listed: "Clickjacking, without additional details demonstrating a specific exploit" |

**Verdict: INVALID**
- **Reason:** Out-of-scope security header best practices; "Clickjacking, without additional details demonstrating a specific exploit" explicitly excluded

---

## Lead 8: dev.derdack.com Dot-Prefix Sensitive Paths Served

**Asset:** `dev.derdack.com`

| # | Answer |
|---|--------|
| Q1 | **Yes** |
| Q2 | **Yes** — Apache serves these paths |
| Q3 | **Weak** — `.ssh/` returns 403 (directory exists but blocked), `.bash_history/` and `.viminfo/` return 404 |
| Q4 | **Yes** — passive GET |
| Q5 | **Yes** — but impact minimal |
| Q6 | **No** |
| Q7 | **No** — bigpickle already rejected; 403/404 = no data disclosure |

**Verdict: INVALID**
- **Reason:** All sensitive paths return 403 or 404; no file content disclosed; minimal impact

---

## Summary

| # | Lead | Verdict | CVSS |
|---|------|---------|------|
| 1 | Cross-env JWKS key reuse | **VALID** | 8.1 |
| 2 | Password grant on staging IdP | **VALID** | 7.3 |
| 3 | dev.derdack.com MultiViews | INVALID | — |
| 4 | WP REST API user enum | INVALID | — |
| 5 | /de/ XML-RPC exposed | **VALID** | 7.5 |
| 6 | HTTPS→HTTP downgrade | INVALID | — |
| 7 | Missing security headers | INVALID | — |
| 8 | Dot-prefix path echo | INVALID | — |

**Actionable leads: 1, 2, 5** — all passive, provable non-invasively, and within scope.
